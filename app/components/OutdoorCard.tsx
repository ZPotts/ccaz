interface OutdoorCardProps {
    img: string;
    title: string;
    description: string;
    buttonText: string;
}

const PropertyCard: React.FC<OutdoorCardProps> = ({ img, title, description, buttonText}) => {
    return(
        <div>
            
        </div>
    )
}

export default PropertyCard